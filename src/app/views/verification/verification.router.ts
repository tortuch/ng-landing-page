import { Routes } from '@angular/router';

import { VerificationComponent } from './verification.component';

export const VerificationRouter: Routes = [
    {
        path: '',
        component: VerificationComponent
    }
];
