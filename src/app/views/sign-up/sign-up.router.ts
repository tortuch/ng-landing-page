import { Routes } from '@angular/router';

import { FormComponent } from './form/form.component';

export const SignUpRouter: Routes = [
    {
        path: '',
        component: FormComponent
    }
];
